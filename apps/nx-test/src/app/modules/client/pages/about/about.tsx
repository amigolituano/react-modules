import React from 'react';

import './about.scss';

/* eslint-disable-next-line */
export interface AboutClientProps {}

export const AboutClient = (props: AboutClientProps) => {
  return (
    <div>
      <h1>Welcome to client about!</h1>
    </div>
  );
};

export default AboutClient;
